---
import { getAuth, onAuthStateChanged } from "firebase/auth";
import { auth } from "../lib/firebase";
import StudentSidebar from "../components/StudentSidebar.astro";
import ChatButton from "../components/ChatButton.astro";

const { title, selectedItem } = Astro.props;

// We can add authentication check here if needed
// For now, we'll keep it simple
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title ? `${title} | Student Portal` : "Student Portal"}</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body>
    <div class="student-layout">
      <StudentSidebar selectedItem={selectedItem} />
      
      <main class="student-content">
        <slot />
      </main>
    </div>

    <ChatButton />

    <script>
      import { onAuthStateChanged } from "firebase/auth";
      import { auth } from "../lib/firebase";
      
      // Check if user is authenticated
      onAuthStateChanged(auth, (user) => {
        if (!user) {
          // Redirect to login if not authenticated
          // But allow access to the login page itself
          const currentPath = window.location.pathname;
          if (currentPath !== "/login") {
            window.location.href = "/login";
          }
        }
      });
    </script>
    
    <style>
      :root {
        --color-bg: #1c1c1c;
        --color-text: #ffffff;
        --color-text-muted: rgba(255, 255, 255, 0.7);
        --color-primary: rgb(139, 0, 0);
        --color-primary-hover: rgba(139, 0, 0, 0.8);
        --color-card-bg: #1a1c25;
        --color-border: rgba(255, 255, 255, 0.1);
        --color-hover-bg: rgba(255, 255, 255, 0.05);
        --color-success: #10b981;
        --color-warning: #f59e0b;
        --color-danger: #ef4444;
      }
      
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }
      
      body {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
        background-color: var(--color-bg);
        color: var(--color-text);
        line-height: 1.6;
        min-height: 100vh;
      }
      
      .student-layout {
        display: flex;
        min-height: 100vh;
        position: relative;
      }
      
      .student-content {
        flex: 1;
        overflow-y: auto;
        padding: 2rem;
        max-width: calc(100% - 280px);
      }
      
      @media (max-width: 992px) {
        .student-content {
          max-width: calc(100% - 240px);
        }
      }
      
      @media (max-width: 768px) {
        .student-layout {
          flex-direction: column;
        }
        
        .student-content {
          padding: 1.5rem;
          max-width: 100%;
        }
      }
    </style>
  </body>
</html>